# (cros-piskel) Chrome OS Piskel App
<br><br><br>

Installation
============
- To create the .CRX file for installation: go to `chrome://extensions` page, select `PACK EXTENSION`, select `BROWSE` and choose the repository folder.
- Drag the .CRX file to `chrome://extensions` page and drop.
